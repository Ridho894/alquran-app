import React from "react";
import { ScrollView, View, Text } from "react-native";

const NewsCard = () => {
  return (
    <ScrollView>
      <View>
        <Text>News Card</Text>
      </View>
    </ScrollView>
  );
};

export default NewsCard;
